import React from "react";
import ProfileStructure from "../../templates/ProfileStructure";

export default function ProfileScreen ({ navigation }: any) {
    return (
        <ProfileStructure navigation={navigation} />
    );
}